# 🧪 [Testing] Add tests for VisitCalendar component

## 🎯 What
Added missing unit tests for the `VisitCalendar` component in `src/app/stats/components/VisitCalendar.tsx`.

## 📊 Coverage
The following scenarios are now covered:
- Proper rendering with light and dark themes.
- Correct handling of click events, invoking `setDate`.
- Correct visual indicators (dots) and screen reader text for dates with visits.
- Missing visual indicators for dates without visits.

## ✨ Result
Improved test coverage and reliability for the stats view. Ensured the calendar component renders and behaves exactly as expected with different themes and data states.
