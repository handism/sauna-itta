# 🧪 Add tests for TopSaunasCard

## 🎯 What
Added missing tests for the `TopSaunasCard` component.

## 📊 Coverage
- Empty state rendering (returns `null`).
- Rendering the list of top saunas with correct details (name, count, area, rating).
- Slicing logic (rendering only the top 5).
- Handling conditional rendering of meta information (no area/rating).

## ✨ Result
Increased test coverage and confidence in the component's rendering logic.
