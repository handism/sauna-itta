# プルリクエスト・レビューまとめ (2026-03-16)

リポジトリ内のオープンなプルリクエスト（PR #1, #2, #10, #11）を順番にレビューした結果をまとめます。

## PR #1: Initial feature set
- **ステータス**: クローズまたは大幅なリベースを推奨
- **レビュー内容**:
  - このPRは、現在のモジュール化されたコードベース（`src/components/sauna-map/` 配下への分割後）とは非常に古い地点で分岐しています。
  - `main` ブランチに対して 5,000 行以上の削除と 1,900 行の追加が含まれており、実質的に最新のリファクタリングを元に戻す（デグレードさせる）形になっています。
  - 現在のアーキテクチャに合わせた機能追加を行う場合は、最新の `main` から再度ブランチを作成することを強く推奨します。

## PR #2: Proposed issues and documentation
- **ステータス**: 部分的に有効（一部は実装済み）
- **レビュー内容**:
  - `docs/proposed_issues/` に以下の機能提案ドキュメントを追加する内容です。
    - 001-location-search (地点検索) - **未実装・有用**
    - 002-sauna-specs (詳細スペック) - **未実装・有用**
    - 003-visit-history (訪問履歴) - **実装済み**
    - 004-calendar-view (カレンダー) - **実装済み**
    - 005-image-compression (画像圧縮) - **実装済み**
    - 006-pwa-support (PWA対応) - **未実装・有用**
  - 実装済みの項目については、すでにリポジトリ内の `logs/` や機能として取り込まれているため、未実装の提案のみを抽出してマージするか、ドキュメントを最新化する必要があります。

## PR #10: issue-8-visit-calendar
- **ステータス**: マージ不要（反映済み）
- **レビュー内容**:
  - `main` ブランチには、すでに `stats` ページのカレンダー機能が含まれています。
  - `main` ブランチ側で CI ビルドエラー（TypeScriptの型エラー）を解消するための修正（`useState<Date | null>` の導入など）がすでに行われており、PR #10 の現在の状態（`useState<Date | Date[]>`）をそのままマージするとビルドエラーが再発します。
  - すでに `main` に意図した機能が安定した形で取り込まれているため、このPRはそのままクローズして問題ありません。

## PR #11: issue-6-visit-history
- **ステータス**: マージ不要（反映済み）
- **レビュー内容**:
  - 訪問履歴機能についても、主要なロジック（`buildHistoryUpdate` への切り出し、`removeLastHistoryEntry` の追加など）は `main` ブランチに反映されています。
  - 履歴リストの `key` 指定において、PR #11 では `index` を使用していますが、`main` では `date-index` を使用するように調整されています。
  - 必要な機能と指摘への対応はすべて `main` に反映済みのため、このPRもクローズ可能です。

---
**総評**:
リポジトリの履歴を確認したところ、PR #10 と #11 の内容はすでに `main` に統合されており、一部の微修正も完了しています。PR #1 は極めて古いバージョンに基づいているため、現在の開発ラインとは互換性がありません。唯一、PR #2 に含まれる将来の機能提案ドキュメントは、プロジェクトのロードマップとして価値があります。
